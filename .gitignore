node_modules/
.vscode/
build/
.DS_Store
html_tailwind/*
.env

composer.lock
vendor/
package-lock.json
yarn.lock
plans

bundle/

# Project-specific AI-generated blocks & components.
# The theme baseline is the core `mbn-` blocks; everything under the `mbn-ai-`
# namespace is generated per project/design (via /build-design, /build-components)
# and reset by /cleanup, so it is not committed to the reusable theme.
src/mbn-ai-*/
src/components/mbn-ai-*/

# vexp local index (built by the vexp daemon; machine-local)
.vexp/